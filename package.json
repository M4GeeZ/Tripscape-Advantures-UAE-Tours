{
  "name": "tripscape-premium-mern",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "prepare:images": "node scripts/generate-image-manifest.mjs",
    "dev": "npm run prepare:images && concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run prepare:images && npm run build --prefix client",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
